import { ValueInputDirective } from './value-input.directive';

describe('ValueInputDirective', () => {
  it('should create an instance', () => {
    // const directive = new ValueInputDirective();
    // expect(directive).toBeTruthy();
  });
});
